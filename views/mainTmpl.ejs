<!DOCTYPE html>
<html lang="zh-cn">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0, user-scalable=0">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
	<title>饭掌柜系统</title>

    <!-- Bootstrap -->
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
	<body>
		<div class="container-fluid" id="all" style="margin-top:50px">
			<div class="col-sm-12">
				<div class="panel panel-primary">
					<div class="panel-heading" ng-controller="HeadCtrl">
						<h1>
							<%= typeName %>当前订单：
							<button class="btn btn-lg btn-danger pull-right" onclick="location.href='/account/signout'">退出</button>
							<button class="btn btn-lg btn-default pull-right" style="margin-right:5px" ng-click="full()" ng-hide="isInFullScreen">全屏</button> 
						</h1>
					</div>
					<div class="panel-body">
						<div class="container-fluid" ng-controller="PackCtrl">
							<div id="groups">
								<p ng-if="packages.length == 0">目前还没有订单</p>
								<div class="col-xs-4 col-md-2 ng-cloak" ng-repeat-start="pack in packages track by $index" ng-cloak>
									<div class="thumbnail">
										<img class="img-responsive" ng-src="/image/{{pack.image}}/middle" />
										<div class="caption">
											<h4>{{pack.name}}</h4>
											<p>{{pack.count}}份</p>
											<p class="text-info" am-time-ago="pack.time"></p>
											<p>
												<img ng-if="pack.uid" style="height:20px" ng-src="/port/{{pack.uid}}/thumb" />
												<span ng-if="pack.nick" class="text-primary">{{pack.nick}} 的订单</span>
											</p>
											<p ng-if="pack.location" class="well well-sm">地点： {{pack.location}}</p>
											<p ng-if="pack.extra" class="well well-sm">选项： {{pack.extra}}</p>
											<button class="btn btn-success" ng-click="remind(pack.gid)">提醒取餐</button>
										</div>
									</div>
								</div>
								<div class="clearfix visible-sm-block visible-xs-block" ng-if="($index + 1 ) % 3 == 0" ng-repeat-end></div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
		<footer style="text-align: center">
			<small class="text-muted">&copy;2015 intel成都</small>
		</footer>
		<script src="/jquery-1.11.2.min.js"></script>
		<script src="/bootstrap/js/bootstrap.min.js"></script>
		<script src="/assets/kitchen.js"></script>
	</body>
</html>
