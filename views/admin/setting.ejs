<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0, user-scalable=0">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
	<title>饭掌柜系统</title>

    <!-- Bootstrap -->
    <link href="/renders/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/adminSetting.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
	<body>
		<div class="container" style="margin-top: 30px">
			<% include ../messages %>
			<div class="panel <%= valve ? 'panel-danger' : 'panel-success' %>">
				<a class="btn btn-<%= valve ? 'success' : 'danger' %> pull-right" href="toggle/1" style="margin-right:10px">
					<%= !valve ? '打烊' : '营业' %>
				</a>
				<a class="btn btn-default pull-right" href="/admin" style="margin-right:10px">返回</a>
				<div class="panel-heading">
					<h3 class="panel-title">当前<%= valve ? '打烊' : '营业' %>中</h3>
				</div>
				<div class="panel-body">
					<p class="lead">设置本店外送时间(格式如9:00, 不填写则清空设置)</p>
					<form action="setDeliveryTime" method="POST" class="form-inline">
						<div class="form-group form-group-inline">
							<label for="start">开始外送时间</label>
							<input type="text" class="form-control" id="start" name="start" placeholder="无限制" value="<%= start %>">
						</div> 
						<div class="form-group form-group-inline">
							<label for="end">结束外送时间</label>
							<input type="text" class="form-control" id="end" name="end" placeholder="无限制" value="<%= end %>">
						</div>
						<div class="form-group form-group-inline">
							<button type="submit" class="btn btn-primary">设置</button>
						</div>
					</form>
					<hr>
					<p class="lead">设置本店起送价格(不填写则清空设置)</p>
					<form action="setDeliveryCharge" method="POST" class="form-inline">
						<div class="form-group form-group-inline">
							<label for="minCharge">起送价格</label>
							<input type="text" class="form-control" id="minCharge" name="minCharge" placeholder="无限制" value="<%= minCharge %>">
						</div> 
						<div class="form-group form-group-inline">
							<button type="submit" class="btn btn-primary">设置</button>
						</div>
					</form>
					
				</div>
			</div>
		</div>
		
		
		<footer class="text-center">
			<small class="text-muted">&copy;2015 intel成都</small>
		</footer>
		<script src="/js/jquery-1.11.2.min.js"></script>
		<script src="/renders/bootstrap/js/bootstrap.min.js"></script>
		<script src="/js/adminSetting.js"></script>
	</body>
</html>
